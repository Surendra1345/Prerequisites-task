[project]
name = "prerequisites-task"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "httpx>=0.28.1",
    "pydantic>=2.13.4",
    "ruff>=0.16.2",
    "uvicorn>=0.52.1",
]

[project.scripts]
prerequisites-task = "prerequisites_task:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
